# Screenshots

The README references three images that live in this folder:

- `similar-insights-record.png` — the semantic similar-insights component on a MedicalInsight record page
- `pattern-alerts-home.png` — the pattern alerts card on the home page
- `intent-fields.png` — the Details tab of a MedicalInsight showing the intent fields populated

Recommended dimensions: 1400–1800px wide, cropped tightly to just the component (no full-browser chrome).
